# Hoist Temporal's modules to the project root so webpack's workflow
# bundler can resolve @temporalio/workflow from apps/worker/src. Without
# this, pnpm stashes them behind .pnpm/ and the bundler fails with
# "Can't resolve '@temporalio/workflow/lib/worker-interface.js'".
public-hoist-pattern[]=@temporalio/*
